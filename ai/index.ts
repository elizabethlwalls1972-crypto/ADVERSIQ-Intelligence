/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * ADVERSIQ INTELLIGENCE — OMNI-NODE ARCHITECTURE INDEX
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 * Central barrel export for the entire Omni-Node cognitive architecture.
 *
 * Modules:
 *   - QuorumGatekeeper      — Dynamic persona spawning (Infinite Bench)
 *   - AutonomousSwarm        — Recursive web research sub-agents
 *   - AdversarialSelfPlay    — 24/7 background strategy generation
 *   - AlgorithmicMutator     — Self-writing mathematical formulas
 *   - AdversiqOmniNode       — Central Nervous System (root daemon)
 *   - judges                 — Safety & math audit interfaces
 *
 * Supporting Modules:
 *   - CognitiveUniverseEngine — Jungian archetypes + emergent complexity
 *   - MorphicFieldEngine      — Federated weight averaging + cosine resonance
 *   - llmGateway              — Unified LLM provider routing
 *   - osintTools              — Web search + PDF scraping for the swarm
 *
 * Security: CONFIDENTIAL / FOR STRATEGIC DEPLOYMENT
 * ═══════════════════════════════════════════════════════════════════════════════
 */

export { QuorumGatekeeper } from './QuorumGatekeeper';
export { AutonomousSwarm } from './AutonomousSwarm';
export { AdversarialSelfPlay } from './AdversarialSelfPlay';
export { AlgorithmicMutator } from './AlgorithmicMutator';
export { AdversiqOmniNode } from './AdversiqOmniNode';
export { Judge1SafetyCheck, Judge2MathCheck } from './judges';
